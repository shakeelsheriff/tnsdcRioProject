version: '3'
services:
  rioproject:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      ports:
  - 8081:8080
    volumes:
      - jenkins_data:/var/lib/jenkins
    networks:
      - app-network

  tomcat:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - 80:80
    networks:
      - app-network

  eclipse:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      - DISPLAY=:1
    ports:
      - 5901:5901
    volumes:
      - workspace:/workspace
    networks:
      - app-network

volumes:
  jenkins_data:
  workspace:

networks:
  app-network:
    driver: bridge
